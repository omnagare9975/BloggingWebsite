<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Om Nagare Blog's | All Blogs</title>
  <style>
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #0d0d0d;
      color: #eaeaea;
    }
    .navbar {
      background-color: #000;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid #222;
      position: sticky;
      top: 0;
      z-index: 999;
    }
    .navbar .logo {
      font-size: 1.8rem;
      color: #00bcd4;
      font-weight: bold;
      text-decoration: none;
    }
    .navbar .nav-links a {
      color: #eaeaea;
      margin-left: 1.5rem;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
    }
    .navbar .nav-links a:hover {
      color: #00bcd4;
    }
    .blog-list {
      padding: 3rem 2rem;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
    }
    .blog-card {
      background: #1a1a1a;
      padding: 1.5rem;
      border-radius: 12px;
      border: 1px solid #333;
      transition: 0.3s ease;
    }
    .blog-card:hover {
      border-color: #00bcd4;
      transform: translateY(-5px);
    }
    .blog-card h3 {
      color: #00bcd4;
      margin-top: 0;
    }
    .blog-card p {
      color: #aaa;
    }
    .footer {
      background-color: #000;
      padding: 1.2rem;
      text-align: center;
      color: #666;
      font-size: 0.9rem;
      border-top: 1px solid #222;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar">
    <a href="/" class="logo">Om Nagare Blog's</a>
    <div class="nav-links">
      <a href="/home">Home</a>
      <a href="/user/login">Sign In</a>
      <a href="/user/signup">Sign Up</a>
      <a href="/addblog">Add Blogs</a>
    </div>
  </nav>

  <!-- Blogs Section -->
  <section class="blog-list">
    <% blogs.forEach(blog => { %>
      <div class="blog-card">
        <h3><%= blog.title %></h3>
        <p><%= blog.content.substring(0, 100) %>...</p> <!-- Show a short snippet of the blog content -->
        <a href="/blog/<%= blog._id %>">Read More</a> <!-- Link to individual blog page -->
      </div>
    <% }); %>
  </section>

  <!-- Footer -->
  <footer class="footer">
    &copy; <%= new Date().getFullYear() %> Om Nagare Blog's. All rights reserved.
  </footer>
</body>
</html>
